name: Legacy Deploy Sedi Backend (DISABLED)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disable notice
        run: |
          echo "This workflow has been disabled for safety."
          echo "Reason: It deploys to old root server (91.107.168.130) and bypasses Docker-based Cloud.ir architecture."
          echo "Use Test Server SSH + new Docker deploy workflow instead."
