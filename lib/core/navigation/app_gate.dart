/// Frontend gate identifiers for the 3-gate Sedi app architecture.
enum SediAppGate {
  /// Gate 1 — Welcome / Splash (`IntroPage`).
  splash,

  /// Gate 2 — Login / Registration (`OtpLoginPage`).
  login,

  /// Gate 3 — Sedi Heart / main home (`ChatPage`).
  heart,
}
