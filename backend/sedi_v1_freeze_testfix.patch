diff --git a/backend/tests/acceptance/test_stage16_6_push_notifications.py b/backend/tests/acceptance/test_stage16_6_push_notifications.py
index 0000000..0000000 100644
--- a/backend/tests/acceptance/test_stage16_6_push_notifications.py
+++ b/backend/tests/acceptance/test_stage16_6_push_notifications.py
@@ -31,12 +31,18 @@ def test_user(db: Session) -> User:
     return user
 
 
+# Realistic FCM token: >=80 chars, no placeholder words (passes _is_placeholder_or_invalid_fcm_token)
+_REGISTER_TEST_FCM_TOKEN = (
+    "abcdefghijklmnopqrstuvwxyz0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789abcdefghijklmnopqrstuv"
+)
+
 def test_push_register_upsert(client: TestClient, db: Session, test_user: User):
     """POST /notifications/push/register upserts by fcm_token; returns device_id."""
     body = {
         "user_id": test_user.id,
         "platform": "android",
-        "fcm_token": "test-fcm-token-12345",
+        "fcm_token": _REGISTER_TEST_FCM_TOKEN,
         "device_id": "device-abc",
     }
@@ -62,11 +68,11 @@ def test_push_register_upsert(client: TestClient, db: Session, test_user: User):
 
 def test_queued_to_sent_with_mock_fcm(client: TestClient, db: Session, test_user: User):
     """With FCM_DISABLED=true, a queued notification with active PushDevice transitions to sent."""
-    # Register push device
+    # Register push device (token must pass validation: >=80 chars, no placeholder words)
     device = PushDevice(
         user_id=test_user.id,
         platform="android",
-        fcm_token="mock-fcm-token-xyz",
+        fcm_token=_REGISTER_TEST_FCM_TOKEN,
         is_active=True,
     )
diff --git a/backend/app/decision_engine/models.py b/backend/app/decision_engine/models.py
index 0000000..0000000 100644
--- a/backend/app/decision_engine/models.py
+++ b/backend/app/decision_engine/models.py
@@ -1,6 +1,6 @@
 # Release D: minimal Decision schema + Event/Actions for unified path
 from dataclasses import dataclass
 from datetime import datetime
-from pydantic import BaseModel
+from pydantic import BaseModel, Field
 from typing import Literal, Optional, Dict, Any, List, Union
 
@@ -17,7 +17,11 @@ class EventDto(BaseModel):
     event_type: str
     payload: Dict[str, Any]
     recorded_at: Optional[datetime] = None
-    received_at: datetime  # Required: when the server received the event
+    received_at: datetime = Field(
+        default_factory=datetime.utcnow,
+        description="When the server received the event; defaults to now if omitted (backward-compatible for tests).",
+    )
     event_id: Optional[int] = None
diff --git a/backend/tests/test_e2e_abnormal_hr.py b/backend/tests/test_e2e_abnormal_hr.py
index 0000000..0000000 100644
--- a/backend/tests/test_e2e_abnormal_hr.py
+++ b/backend/tests/test_e2e_abnormal_hr.py
@@ -80,5 +80,13 @@ def test_e2e_abnormal_hr_creates_health_alert(
     assert notif.body is not None and len(notif.body.strip()) > 0
     assert notif.priority in ("high", "critical")
-    # high_heart_rate is the alert_code for bpm > 120 (rule_alerts)
-    assert "heart" in notif.body.lower() or "high_heart_rate" in (notif.dedupe_key or "")
+    # V1: body may be Persian (e.g. "ضربان قلبت بالاست"); dedupe_key may contain heart_rate_high or high_heart_rate
+    body_lower = notif.body.lower()
+    dedupe = notif.dedupe_key or ""
+    assert (
+        "heart" in body_lower
+        or "ضربان" in notif.body
+        or "قلب" in notif.body
+        or "heart_rate_high" in dedupe
+        or "high_heart_rate" in dedupe
+    ), f"Expected heart-related body or dedupe_key; got body={notif.body[:80]!r} dedupe_key={dedupe!r}"
diff --git a/backend/docs/contracts/v1/notifications.md b/backend/docs/contracts/v1/notifications.md
index 0000000..0000000 100644
--- a/backend/docs/contracts/v1/notifications.md
+++ b/backend/docs/contracts/v1/notifications.md
@@ -102,8 +102,12 @@
 **Success (200):** `ApiResponse` with `data` indicating registration result.
 **Error (200):** Invalid token or user: `{"ok": false, "error": {"code": "USER_NOT_FOUND", "message": "..."}}`.
 
-**Request body (example):**
+**Request body (example):**  
+FCM token must be at least 80 characters and must not be a placeholder (e.g. no "TOKEN", "FCM", "EXAMPLE" in value).
 ```json
 {
   "user_id": 1,
-  "fcm_token": "long-fcm-token-string",
+  "fcm_token": "abcdefghijklmnopqrstuvwxyz0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789abcdefghijklmnopqrstuv",
   "platform": "android",
   "device_id": "optional-device-id"
 }
