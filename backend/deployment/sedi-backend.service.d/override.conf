[Service]
# Reset original ExecStart from base unit
ExecStart=

# V1: legacy ingest auth (X-DEVICE-TOKEN)
Environment="DEVICE_AUTH_MODE=legacy_only"
Environment="DEVICE_INGEST_TOKEN=K9mN2pQ5rT8vY1zA4bC7dE0fG3hJ6kL"

ExecStart=/var/www/sedi/backend/.venv/bin/uvicorn backend.app.main:app --host 0.0.0.0 --port 8000
