[Service]
# Reset original ExecStart from base unit
ExecStart=

# Inject DEVICE_AUTH_MODE directly into process environment (V1 path)
ExecStart=/usr/bin/env DEVICE_AUTH_MODE=legacy_only /var/www/sedi/backend/.venv/bin/uvicorn backend.app.main:app --host 0.0.0.0 --port 8000
