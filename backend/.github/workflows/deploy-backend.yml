name: Legacy Deploy Sedi Backend (DISABLED — nested copy)

# GitHub Actions only runs workflows from the repository root `.github/workflows/`.
# This nested file is intentionally disabled. Do not reactivate.
# Production deploy: use root `.github/workflows/deploy-backend-ghcr.yml` (manual workflow_dispatch).

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Disable notice
        run: |
          echo "This nested workflow has been disabled for safety."
          echo "Reason: It targeted the legacy server (91.107.168.130) via systemd/git pull."
          echo "Use the repository root workflows instead:"
          echo "  - .github/workflows/deploy-backend-ghcr.yml (production deploy from GHCR)"
          echo "  - .github/workflows/test-server-ssh.yml (SSH connectivity test)"
