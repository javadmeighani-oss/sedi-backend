# تحلیل نیاز به دیتابیس برای سوالات متفرقه کاربر

**تاریخ:** 2024-12-30  
**موضوع:** بررسی نیاز به ساخت دیتابیس برای مدیریت سوالات متفرقه کاربر

---

## وضعیت فعلی سیستم

### 1. سیستم فعلی چگونه کار می‌کند؟

**پاسخ‌دهی به سوالات:**
- ✅ از **GPT-4o-mini** برای پاسخ به همه سوالات استفاده می‌شود
- ✅ **System Prompt** راهنمایی می‌کند که چگونه پاسخ دهد
- ✅ **Conversation History** در جدول `Memory` ذخیره می‌شود
- ✅ برای سوالات غیر مرتبط با سلامت، prompt خاصی وجود دارد (`unrelated_question`)

**ذخیره‌سازی:**
- ✅ جدول `Memory` تمام گفتگوها را ذخیره می‌کند:
  - `user_message`: پیام کاربر
  - `sedi_response`: پاسخ صدی
  - `language`: زبان گفتگو
  - `created_at`: زمان

**مدیریت سوالات:**
- ✅ سوالات درباره صدی: از GPT استفاده می‌کند (`_answer_sedi_question_with_guidance`)
- ✅ سوالات غیر مرتبط: prompt خاص که کاربر را به سلامت هدایت می‌کند
- ✅ سوالات پزشکی: prompt خاص با هشدار

---

## آیا نیاز به دیتابیس اضافی هست؟

### تحلیل سناریوها

#### سناریو 1: سوالات متداول (FAQ)

**سوال:** آیا نیاز به دیتابیس FAQ هست؟

**پاسخ فعلی:**
- ❌ **نیاز نیست** - GPT به اندازه کافی هوشمند است
- ✅ GPT می‌تواند به سوالات متداول پاسخ دهد
- ✅ System Prompt راهنمایی می‌کند

**اگر بخواهیم FAQ اضافه کنیم:**
- ✅ **مزایا:** پاسخ‌های سریع‌تر و دقیق‌تر
- ✅ **مزایا:** کاهش هزینه API calls
- ❌ **معایب:** نیاز به نگهداری و به‌روزرسانی
- ❌ **معایب:** پیچیدگی بیشتر

**توصیه:** 
- **فعلاً نیاز نیست** - GPT کافی است
- **در آینده:** اگر سوالات تکراری زیاد شد، می‌توانیم FAQ اضافه کنیم

---

#### سناریو 2: RAG (Retrieval Augmented Generation)

**سوال:** آیا نیاز به RAG برای بهبود پاسخ‌ها هست؟

**پاسخ فعلی:**
- ❌ **نیاز نیست** - GPT به تنهایی کافی است
- ✅ Conversation History در `Memory` ذخیره می‌شود
- ✅ Context از تاریخچه گفتگو استفاده می‌کند

**اگر بخواهیم RAG اضافه کنیم:**
- ✅ **مزایا:** پاسخ‌های دقیق‌تر بر اساس اطلاعات ذخیره شده
- ✅ **مزایا:** استفاده از اطلاعات قبلی کاربر
- ❌ **معایب:** پیچیدگی بالا
- ❌ **معایب:** نیاز به vector database (مثل Pinecone, Weaviate)

**توصیه:**
- **فعلاً نیاز نیست** - سیستم فعلی کافی است
- **در آینده:** اگر نیاز به پاسخ‌های دقیق‌تر بر اساس اطلاعات قبلی بود، RAG می‌تواند مفید باشد

---

#### سناریو 3: دسته‌بندی و تحلیل سوالات

**سوال:** آیا نیاز به دسته‌بندی سوالات هست؟

**پاسخ فعلی:**
- ✅ **تا حدی وجود دارد** - سیستم سوالات را تشخیص می‌دهد:
  - سوالات درباره صدی (`non_name_question`)
  - سوالات غیر مرتبط (`unrelated_question`)
  - سوالات پزشکی (`medical_question`, `early_medical_question`)

**اگر بخواهیم دسته‌بندی کامل اضافه کنیم:**
- ✅ **مزایا:** تحلیل بهتر رفتار کاربر
- ✅ **مزایا:** بهبود پاسخ‌ها بر اساس دسته
- ❌ **معایب:** نیاز به دیتابیس اضافی
- ❌ **معایب:** پیچیدگی بیشتر

**توصیه:**
- **فعلاً نیاز نیست** - سیستم فعلی کافی است
- **در آینده:** اگر نیاز به تحلیل و گزارش‌گیری بود، می‌توانیم دسته‌بندی اضافه کنیم

---

#### سناریو 4: ذخیره سوالات و پاسخ‌های متداول

**سوال:** آیا نیاز به ذخیره سوالات متداول هست؟

**پاسخ فعلی:**
- ✅ **تا حدی وجود دارد** - تمام گفتگوها در `Memory` ذخیره می‌شوند
- ✅ می‌توان از `Memory` برای تحلیل استفاده کرد

**اگر بخواهیم جدول جداگانه اضافه کنیم:**
- ✅ **مزایا:** دسترسی سریع‌تر به سوالات متداول
- ✅ **مزایا:** امکان ساخت FAQ خودکار
- ❌ **معایب:** تکراری بودن با `Memory`
- ❌ **معایب:** نیاز به نگهداری

**توصیه:**
- **فعلاً نیاز نیست** - `Memory` کافی است
- **در آینده:** اگر نیاز به FAQ خودکار بود، می‌توانیم از `Memory` استفاده کنیم

---

## نتیجه‌گیری و توصیه

### ✅ **نیاز نیست - سیستم فعلی کافی است**

**دلایل:**
1. **GPT به اندازه کافی هوشمند است** - می‌تواند به سوالات متفرقه پاسخ دهد
2. **System Prompt راهنمایی می‌کند** - نحوه پاسخ دادن مشخص است
3. **Conversation History ذخیره می‌شود** - در جدول `Memory`
4. **Context از تاریخچه استفاده می‌کند** - پاسخ‌ها بر اساس گفتگوهای قبلی هستند

### 🔮 **در آینده (اگر نیاز شد):**

#### 1. FAQ Database (اولویت پایین)
```python
class FAQ(Base):
    __tablename__ = "faq"
    
    id = Column(Integer, primary_key=True)
    question = Column(String, nullable=False)
    answer = Column(String, nullable=False)
    category = Column(String)  # "sedi", "health", "general"
    language = Column(String)  # "en", "fa", "ar"
    usage_count = Column(Integer, default=0)
    created_at = Column(DateTime, default=datetime.utcnow)
```

**استفاده:**
- قبل از GPT، چک کنیم آیا سوال در FAQ هست
- اگر بود، از FAQ استفاده کنیم (سریع‌تر و ارزان‌تر)
- اگر نبود، از GPT استفاده کنیم

#### 2. Question Categories (اولویت پایین)
```python
class QuestionCategory(Base):
    __tablename__ = "question_categories"
    
    id = Column(Integer, primary_key=True)
    user_id = Column(Integer, ForeignKey("users.id"))
    question = Column(String, nullable=False)
    category = Column(String)  # "about_sedi", "unrelated", "medical", "health"
    created_at = Column(DateTime, default=datetime.utcnow)
```

**استفاده:**
- تحلیل رفتار کاربر
- بهبود پاسخ‌ها بر اساس دسته
- گزارش‌گیری

#### 3. RAG System (اولویت پایین)
- استفاده از Vector Database (Pinecone, Weaviate)
- Embedding سوالات و پاسخ‌ها
- جستجوی مشابه برای پاسخ بهتر

---

## توصیه نهایی

### ✅ **فعلاً نیاز نیست**

**دلایل:**
1. سیستم فعلی با GPT کافی است
2. Conversation History در `Memory` ذخیره می‌شود
3. System Prompt راهنمایی می‌کند
4. پیچیدگی اضافه نمی‌شود

### 🔮 **در آینده (اگر نیاز شد):**

**اولویت 1: FAQ Database** (اگر سوالات تکراری زیاد شد)
- کاهش هزینه API
- پاسخ‌های سریع‌تر

**اولویت 2: Question Categories** (اگر نیاز به تحلیل بود)
- تحلیل رفتار کاربر
- بهبود پاسخ‌ها

**اولویت 3: RAG System** (اگر نیاز به پاسخ‌های دقیق‌تر بود)
- استفاده از اطلاعات قبلی
- پاسخ‌های شخصی‌سازی شده

---

## خلاصه

| نیاز | وضعیت فعلی | نیاز به دیتابیس | اولویت |
|------|------------|-----------------|--------|
| **پاسخ به سوالات** | ✅ GPT | ❌ نیاز نیست | - |
| **ذخیره گفتگوها** | ✅ Memory Table | ✅ موجود | - |
| **FAQ** | ❌ ندارد | ⚠️ اختیاری | پایین |
| **دسته‌بندی** | ✅ تا حدی | ⚠️ اختیاری | پایین |
| **RAG** | ❌ ندارد | ⚠️ اختیاری | پایین |

**نتیجه:** سیستم فعلی کافی است. در آینده اگر نیاز شد، می‌توانیم FAQ یا RAG اضافه کنیم.

