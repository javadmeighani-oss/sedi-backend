# بهبود جریان Onboarding رمز عبور

**تاریخ:** 2024-12-30

---

## تغییرات انجام شده

### 1. به‌روزرسانی کانتکس `name_confirmed`

**قبل:**
```
از این به بعد من به عنوان دستیار مراقبت و سلامت همراهت هستم.
برای اینکه از اطلاعاتت محافظت کنم و ارتباطمون امن بمونه،
لازمه یک رمز امنیتی (حداقل ۶ کاراکتر) انتخاب کنی.

لطفاً برای من ارسال کن. منتظرم.
```

**بعد:**
```
{user_name} عزیز از این به بعد من به عنوان دستیار و مشاور مراقبت و سلامت تو همیشه کنارت هستم. فقط قبل از شروع این رابطه، برای اینکه بتونم از اطلاعاتت محافظت کنم و ارتباطمون امن بمونه تو باید یک رمز (حداقل با 6 کاراکتر از حروف و علائم) انتخاب کنی. منتظر ارسال رمز هستم.
```

**تغییرات:**
- اضافه شدن نام کاربر در ابتدای پیام
- تغییر "دستیار" به "دستیار و مشاور"
- تغییر "حداقل ۶ کاراکتر" به "حداقل با 6 کاراکتر از حروف و علائم"
- تغییر لحن به دوستانه‌تر

---

### 2. اضافه شدن کانتکس `password_refusal_acceptance`

**کانتکس جدید:**
```
{user_name} عزیز من لازم است یادآوری کنم که ارائه رمز برای محافظت از حریم شخصی خودت است و هر وقت لازم دونستی میتوانی یک رمز با همان ویژگی که نام بردم (حداقل 6 کاراکتر از حروف و علائم) ایجاد کنی و من آن رمز را ذخیره میکنم.
```

**استفاده:**
- وقتی کاربر دوباره از ارائه رمز امتناع می‌کند
- صدی با درخواست کاربر موافقت می‌کند
- یادآوری می‌کند که می‌تواند در آینده رمز ایجاد کند

---

### 3. بهبود کانتکس `security_gate_active`

**قبل:**
```
{user_name} عزیز،
برای اینکه بتونیم یک ارتباط واقعی و قابل اعتماد داشته باشیم
و از اطلاعات شخصی‌ت محافظت کنم،
لازمه ابتدا یک رمز امنیتی از طرف تو داشته باشم.

لطفاً یک رمز با حداقل ۶ کاراکتر انتخاب کن و برای من بفرست،
بعد از اون همیشه همراه و پشتیبانت هستم.
```

**بعد:**
```
{user_name} عزیز، بدون رمز امنیتی، امکان داره افراد دیگری به حریم خصوصی تو دسترسی داشته باشن و اطلاعات شخصی‌ت در معرض خطر قرار بگیره. برای محافظت از تو و اطلاعاتت، لطفاً یک رمز (حداقل با 6 کاراکتر از حروف و علائم) انتخاب کن و برای من بفرست.
```

**تغییرات:**
- تاکید بیشتر بر مشکلات امنیتی
- توضیح واضح‌تر درباره خطرات
- تغییر لحن به هشداردهنده‌تر

---

### 4. بهبود منطق تشخیص امتناع از رمز

**قابلیت‌های جدید:**
- تشخیص کلمات امتناع در سه زبان (انگلیسی، فارسی، عربی)
- شمارش تعداد امتناع‌ها
- مدیریت دو مرحله‌ای:
  - **اولین امتناع:** نمایش هشدار امنیتی (`security_gate_active`)
  - **دومین امتناع:** پذیرش و یادآوری (`password_refusal_acceptance`)

**کلمات امتناع:**
- **فارسی:** "نه"، "نمی‌خوام"، "لازم نیست"، "نیازی نیست"، "بعداً"، "الان نه"، "رد"، "امتناع"
- **انگلیسی:** "no", "don't", "won't", "refuse", "skip", "later", "not now"
- **عربی:** "لا", "لا أريد", "لست بحاجة", "ليس ضرورياً", "لاحقاً"

---

### 5. بهبود جریان بعد از امتناع

**قبل:**
- کاربر نمی‌توانست بدون رمز ادامه دهد

**بعد:**
- بعد از دومین امتناع، کاربر می‌تواند بدون رمز ادامه دهد
- صدی یادآوری می‌کند که می‌تواند در آینده رمز ایجاد کند
- کاربر وارد تعامل واقعی می‌شود

---

### 6. قابلیت تغییر رمز در آینده

**قابلیت:**
- کاربر می‌تواند در هر زمان رمز جدید ایجاد کند
- صدی کانتکس ارسال دوباره رمز را برای اطمینان از صحت آن بکار می‌برد
- رمز جدید ذخیره می‌شود

---

## جریان کامل Onboarding

### 1. کاربر اسم خود را می‌گوید
→ صدی: `name_confirmed` (با نام کاربر و توضیحات کامل)

### 2. کاربر رمز را ارسال می‌کند
→ صدی: `password_confirm` (درخواست ارسال دوباره برای اطمینان)

### 3. کاربر رمز را دوباره ارسال می‌کند
→ صدی: `password_confirmed` (تایید و ورود به تعامل واقعی)

### 4. کاربر از ارائه رمز امتناع می‌کند (اولین بار)
→ صدی: `security_gate_active` (هشدار امنیتی)

### 5. کاربر دوباره از ارائه رمز امتناع می‌کند
→ صدی: `password_refusal_acceptance` (پذیرش و یادآوری)
→ کاربر می‌تواند بدون رمز ادامه دهد

---

## نتیجه

✅ **تمام نیازمندی‌های کاربر پیاده‌سازی شد:**
- کانتکس `name_confirmed` به‌روزرسانی شد
- کانتکس `password_refusal_acceptance` اضافه شد
- منطق تشخیص امتناع از رمز بهبود یافت
- کاربر می‌تواند بدون رمز ادامه دهد
- قابلیت تغییر رمز در آینده وجود دارد

---

## Deploy

- تغییرات push شدند (commit: `8e61903`)
- GitHub Actions باید به صورت خودکار deploy کند

---

**وضعیت:** ✅ کامل و آماده برای تست

