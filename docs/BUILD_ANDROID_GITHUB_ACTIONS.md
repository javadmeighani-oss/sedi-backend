# SUPERSEDED — Build Android via GitHub Actions

> **SUPERSEDED.** Paths and workflow names in older copies are obsolete.  
> Canonical index: [`FRONTEND_AUTHORITY_INDEX.md`](./FRONTEND_AUTHORITY_INDEX.md)

Current physical debug APK authority is the canonical workflow in `.github/workflows/` on repo `javadmeighani-oss/sedi-frontend` (root Flutter layout).  
Physical builds require real `GOOGLE_SERVICES_JSON` for Firebase project `sedi-b0f08` and package `com.sedi.app`. Placeholder config is not accepted for physical APK builds.

Historical detail moved under `docs/archive/frontend_legacy/` where applicable.
